[project]
name = "theodosia"
version = "0.4.2"
description = "Mount Burr state-machine Applications as MCP servers."
readme = "README.md"
requires-python = ">=3.11,<3.15"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE.md"]
authors = [
    { name = "Adam Munawar Rahman", email = "msrahmanadam@gmail.com" },
]
keywords = [
    "mcp",
    "burr",
    "state-machine",
    "fsm",
    "agent",
    "llm",
    "fastmcp",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries",
    "Typing :: Typed",
]
dependencies = [
    "apache-burr[tracking]>=0.42,<0.43",
    "fastmcp>=3.3,<4.0",
    "pydantic>=2,<3",
    "typer>=0.12,<1.0",
]
# Burr is 0.x; minor versions can break by convention, so we pin to a
# single Burr minor (0.42) and bump deliberately. FastMCP follows semver,
# so the upper bound is the next major (4.0). CI tests against the latest
# patches inside these ranges; minor bumps go through a release with the
# integration retested and called out in CHANGELOG.

[project.urls]
Homepage = "https://github.com/msradam/theodosia"
Repository = "https://github.com/msradam/theodosia"
Documentation = "https://msradam.github.io/theodosia/"
Issues = "https://github.com/msradam/theodosia/issues"
Changelog = "https://github.com/msradam/theodosia/blob/main/CHANGELOG.md"

[project.optional-dependencies]
observability = [
    "apache-burr[opentelemetry]>=0.42,<0.43",
    "opentelemetry-api>=1.20",
    "opentelemetry-sdk>=1.20",
]
ui = [
    "apache-burr[start]>=0.42,<0.43",
]
mellea = [
    "mellea>=0.1",
]
claude = [
    "anthropic>=0.40",
]
all = [
    "theodosia[observability]",
    "theodosia[mellea]",
    "theodosia[ui]",
    "theodosia[claude]",
]

[project.scripts]
theodosia = "theodosia.cli:main"

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/theodosia"]

[dependency-groups]
dev = [
    "pytest>=9",
    "pytest-asyncio>=1.3.0",
    "pytest-cov>=5",
    "hypothesis>=6.100",
    "ruff>=0.15",
    "mypy>=1.10",
    "mutmut>=3.2",
    "theodosia[observability]",
    "psutil>=5.9",
    "bandit>=1.8",
    "detect-secrets>=1.5",
    "sf-hamilton>=1.0,<2.0",
    "claude-agent-sdk>=0.2.82",
]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src"]
extend-exclude = ["examples/data"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "C4", "UP", "SIM", "RUF", "PERF", "PIE", "RET", "FURB"]
# Brand glyphs: ⊢ (turnstile, allowed) and × (multiplication, refused) are
# Theodosia's wire-shape markers. Ruff's ambiguous-Unicode lint flags them as
# look-alikes; that ambiguity is the point of using them as glyphs.
allowed-confusables = ["×", "⊢"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["E402"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
exclude = ["_experimental/"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "strict"
markers = [
    "smoke: integration tests that drive a real Claude session via the Agent SDK against the demo bench (deselected by default; run with `pytest -m smoke`)",
]
addopts = "-m 'not smoke'"

[tool.coverage.run]
source = ["theodosia"]
branch = true

[tool.coverage.report]
show_missing = true
exclude_also = ["if TYPE_CHECKING:", "raise NotImplementedError", "\\.\\.\\."]

# Mutation testing. Starter scope is the security-critical audit ledger; run
# `uv run mutmut run` to check the suite actually kills mutations there (the
# property tests in tests/test_property_ledger.py are the intended killers).
# Not a CI gate (too slow per PR); expand paths_to_mutate as coverage hardens.
[tool.mutmut]
paths_to_mutate = ["src/theodosia/ledger.py"]
# Only the thread-free ledger tests: they are the intended mutation killers.
# Anything that opens a FastMCP client (test_ledger.py's end-to-end cases,
# the CLI runners) leaves live threads behind after mutmut's in-process
# stats run, and forking a threaded process segfaults mutmut's workers on
# macOS.
tests_dir = [
    "tests/test_ledger_chain.py",
    "tests/test_property_ledger.py",
]
# mutmut copies only paths_to_mutate into mutants/, which leaves a broken
# partial `theodosia` namespace package that shadows the installed one; copy
# the whole package so the mutated module imports alongside its siblings,
# and examples/ because conftest.py puts it on sys.path for the test FSMs.
also_copy = ["src/theodosia/", "examples/"]
